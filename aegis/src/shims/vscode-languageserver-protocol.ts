// AEGIS STUB — missing from codeaashu/claude-code leak (SHA 126c311). Throws on use. Replace with real implementation if the feature is needed.

export type InitializeParams = unknown;
export type InitializeResult = unknown;
export type ServerCapabilities = unknown;
export type PublishDiagnosticsParams = unknown;
